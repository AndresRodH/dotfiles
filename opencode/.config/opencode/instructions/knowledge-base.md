# Private knowledge base

When the user brainstorms private ideas, plans, or work directions that should not be exposed to teammates, store and retrieve them via the `knowledge-base` skill and Obsidian vault (`@kb` → ~/Developer/obsidian-vault). Do not write that content into the git worktree unless they explicitly want a shareable repo artifact.

Commands: `/capture` (save), `/kb` (search/read).
