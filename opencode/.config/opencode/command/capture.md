---
description: Capture session ideas/learnings into the private Obsidian vault (not the repo).
agent: build
---
Load the `knowledge-base` skill and capture the following into my private Obsidian vault.

Rules:
- Search for an existing living note first; append if found, else create under PARA (project folder or Inbox).
- Prefer Obsidian CLI; filesystem fallback under ~/Developer/obsidian-vault.
- Do not write this content into the git worktree.
- Include frontmatter (type: session or idea, source: opencode, dates).
- Reply with the note path and a one-line summary of what was saved.

Capture request:
$ARGUMENTS
